services:
  cockpit:
    build: .
    restart: unless-stopped
    ports:
      - "3014:3000"
    volumes:
      - cockpit-data:/data
    env_file:
      - .env
    environment:
      - DATA_DIR=/data
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  cockpit-data:
